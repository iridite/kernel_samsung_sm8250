# Droidian's kernel-snippet.mk auto-appends droidian/$(DEVICE_PLATFORM).config
# (kona) to the fragment chain. Keep this file present so the merge_config.sh
# invocation finds it; device-specific options live in gts7xlwifi.config and
# kona-sec-common.config instead.
