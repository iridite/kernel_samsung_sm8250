# Droidian + Halium kconfig fragment for Samsung Galaxy Tab S7 (gts7lwifi / SM-T870)
# Applied on top of: vendor/kona-perf_defconfig
#                  + vendor/samsung/kona-sec-common.config
#                  + vendor/samsung/gts7lwifi.config

# ── Core Droidian / glibc-userspace requirements ──
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y
CONFIG_VT=y
CONFIG_NAMESPACES=y
CONFIG_MODULES=y
CONFIG_DEVPTS_MULTIPLE_INSTANCES=y
CONFIG_INIT_STACK_ALL_ZERO=y

CONFIG_SYSVIPC=y
CONFIG_PID_NS=y
CONFIG_IPC_NS=y
CONFIG_UTS_NS=y

# ── Halium / libhybris compat ──
# CONFIG_ANDROID_PARANOID_NETWORK is not set
# CONFIG_ANDROID_BINDERFS is not set

# ── USB gadgets — Halium tethering / USB modes ──
CONFIG_USB_CONFIGFS_RNDIS=y
CONFIG_USB_CONFIGFS_RMNET_BAM=y
CONFIG_USB_CONFIGFS_MASS_STORAGE=y

# ── Bluetooth profile stack — bluebinder ──
CONFIG_BT=y
CONFIG_BT_HIDP=y
CONFIG_BT_RFCOMM=y
CONFIG_BT_RFCOMM_TTY=y
CONFIG_BT_BNEP=y
CONFIG_BT_BNEP_MC_FILTER=y
CONFIG_BT_BNEP_PROTO_FILTER=y
CONFIG_BT_HCIVHCI=y

# ── Waydroid (anbox-style binder + networking) ──
CONFIG_ANDROID_BINDER_DEVICES="binder,hwbinder,vndbinder,anbox-binder,anbox-hwbinder,anbox-vndbinder"
CONFIG_SW_SYNC_USER=y
CONFIG_NET_CLS_CGROUP=y
CONFIG_CGROUP_NET_CLASSID=y
CONFIG_VETH=y
CONFIG_NETFILTER_XT_TARGET_CHECKSUM=y

# ── Plymouth boot splash (framebuffer) ──
CONFIG_FB_SYS_FILLRECT=y
CONFIG_FB_SYS_COPYAREA=y
CONFIG_FB_SYS_IMAGEBLIT=y
CONFIG_FB_SYS_FOPS=y
CONFIG_FB_VIRTUAL=y

# ── Crash logging ──
CONFIG_PSTORE=y
CONFIG_PSTORE_CONSOLE=y
CONFIG_PSTORE_RAM=y
CONFIG_PSTORE_RAM_ANNOTATION_APPEND=y

# ── Droidian basics ──
CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
CONFIG_BINFMT_MISC=y
CONFIG_POSIX_MQUEUE=y

# ── Kernel image size reduction ──
# T870 BOOT partition is 71303168 bytes (~68 MB). Must fit uncompressed Image +
# Halium ramdisk + DTB. Samsung ABL doesn't accept compressed kernels.
# CONFIG_DEBUG_INFO is not set
# CONFIG_DEBUG_INFO_BTF is not set
# CONFIG_DEBUG_INFO_DWARF4 is not set
# CONFIG_DEBUG_INFO_DWARF5 is not set
# CONFIG_KALLSYMS_ALL is not set
# CONFIG_FRAME_POINTER is not set
# CONFIG_DEBUG_KERNEL is not set
# CONFIG_PROFILING is not set
# CONFIG_KPROBES is not set
# CONFIG_KGDB is not set
# CONFIG_BLK_DEBUG_FS is not set
# CONFIG_LOCKDEP is not set
# CONFIG_LOCK_STAT is not set

# ── Boot debug (disable PANIC_ON_OOPS so faults are visible) ──
# CONFIG_PANIC_ON_OOPS is not set
CONFIG_PANIC_TIMEOUT=0

# ── IPA3 disabled (WiFi-only tablet, no modem/IPA firmware) ──
# CONFIG_IPA3 is not set
# CONFIG_IPA_WDI_UNIFIED_API is not set
# CONFIG_RMNET_IPA3 is not set
# CONFIG_RNDIS_IPA is not set
# CONFIG_IPA3_MHI_PRIME_MANAGER is not set
# CONFIG_IPA3_REGDUMP is not set

# ── DP re-driver IC (PS5169, for USB-C external display) ──
CONFIG_REDRIVER=y
CONFIG_COMBO_REDRIVER_PS5169=y
